import { Button } from "antd";
import React, { Suspense } from "react";
import "./App.css";

const App: React.FC = () => {
  return (
    <Button type="primary" size="large">
      test
    </Button>
  );
};

export default App;
